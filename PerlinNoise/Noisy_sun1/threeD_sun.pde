// This is a 3D version of the Noisy_Sun program. It's not as cool
// because the fills are opaque. Feel free to edit.


//void draw() {
//  background(0);
//  translate(width/2, height/2);
//  //circle for the center
////  inc = inc + .2;
////  radiusCircle = noise(inc) * 200;
//  radiusCircle = 300;
//  ellipseMode(RADIUS);
//  noStroke();
//  //fill(center[int(random(0,3))]);
//  fill(center[1]);
////  ellipse(0,0,radiusCircle - 6,radiusCircle - 6);
//  lights();
//  sphere(radiusCircle - 6);
//
//  
//  background(0);
//  //for point circles
//  for (int i = 0; i < 1; i++) {
//    if (i == 0) radiusCircle = radiusCircle;
//    else radiusCircle = radiusCircle + (radiusCircle / 8) - 1;
//    for (float angle = 0; angle < 2*PI; angle += .1){
//      for (float angle2 = 0; angle2 < 2*PI; angle2 += .1){
//        if (i == 0) {
//          strokeNum = int(random(0,5));
//          strokeWeight(radiusCircle / 10);
//          stroke(0, 36, 0, 20);
//          x = cos(angle) * sin(angle2) *radiusCircle;
//          y = sin(angle) * sin(angle2) * radiusCircle;
//          z = cos(angle2) * radiusCircle;
//          point(x, y, z);
//        }
//      }
//    }
//   
//   }
//  rotateY(PI/4); 
//}
  

