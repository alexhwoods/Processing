// literally just using the shuffle method from java, 
// I needed to test it out

import java.util.*;

Integer[] array1 = {3,4,5,6,7,8,9,10};
println(array1);
Collections.shuffle(Arrays.asList(array1));
println(array1);
